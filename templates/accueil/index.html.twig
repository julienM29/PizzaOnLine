{% extends 'base.html.twig' %}


{% block body %}
<h1> Je suis dans l'accueil</h1>
    {% for pizza in pizzas %}
<div>
{#   <img src="{{ asset('/images' + pizza.nom) }}"> {{}}#}
    <img src="{{ pizza.urlImage }}" style="width: 250px; height: 200px;" >
    <div>
    {{pizza.nom}}
    </div>
    <div>
    {{pizza.description}}
    </div>
    <div>
    {{pizza.prix}}
    </div>
    <div>
    {% for ingredient in pizza.Ingredients %}
    {{ingredient.nom}}
        {% endfor %}
    </div>
</div>
    {% endfor %}
{% endblock %}
