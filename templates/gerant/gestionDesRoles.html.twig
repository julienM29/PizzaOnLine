{% extends 'base.html.twig' %}

{% block body %}

    <h2 class="flex item-center justify-center text-5xl mt-8 mb-20"> Gestion des collaborateurs</h2>
        <div class="grid grid-cols-4 gap-4 mx-32 mb-32">
    {% for collaborateur in collaborateurs %}
        <div class="bg-gray-200 p-4 w-80 mb-6 flex flex-col justify-between rounded relative">
            {% if collaborateur.sexe == 'Masculin' %}
            <img src="{{ asset('images/pere.png') }}" class="absolute top-0 right-0 px-1.5 py-1.5 w-24 h-24">
            {% elseif collaborateur.sexe == 'Féminin' %}
                <img src="{{ asset('images/petite-fille.png') }}"
                     class="absolute top-0 right-0 px-1.5 py-1.5 w-24 h-24">
            {% else %}
                <img src="{{ asset('images/unisexe.png') }}" class="absolute top-0 right-0 px-1.5 py-1.5 w-24 h-24 ">
            {% endif %}

            <div class=" flex mt-5">
                <div class="flex text-xl  w-1/3 font-bold text-black">Nom : </div>
                <div class="flex text-xl w-1/3 font-semibold text-black"> {{ collaborateur.nom }}</div>
            </div>
            <div class=" flex mt-5">
                <div class="flex text-xl w-1/3 font-bold text-black">Prénom : </div>
                <div class="flex text-xl w-1/3 font-semibold text-black"> {{ collaborateur.prenom }}</div>
            </div>
            <div class=" flex  mt-5">
                <div class="flex text-xl w-1/3  font-bold text-black">Email : </div>
                <div class="flex text-xl w-1/3 font-semibold text-black"> {{ collaborateur.email }}</div>
            </div>
            <div class=" flex  mt-5">
                <div class="flex text-xl w-1/3  font-bold text-black">Sexe : </div>
                <div class="flex text-xl w-1/3 font-semibold text-black"> {{ collaborateur.sexe }}</div>
            </div>
            {% if collaborateur.roles is not empty %}
                <div class="flex mt-5 ">
                    <div class="flex w-1/3 text-xl text-black font-bold">Rôle :</div>
                    <div class="flex-col  w-1/3 font-semibold  text-xl ">
                        {% for role in collaborateur.roles %}
                            <div class="mb-4 text-xl text-black">{{ role }}</div>
                        {% endfor %}
                    </div>
                </div>
            {% endif %}
            <a href="{{ path('_modificationProfil', { 'id': collaborateur.id }) }}"
               class="flex justify-center item-center text-2xl hover:text-red-500 duration-200 text-black font-bold mt-3">Modifier le profil</a>
        </div>


    {% endfor %}
        </div>
{% endblock %}

